from geopy.distance import great_circle

url = "http://api.open-notify.org/iss-now.json"


def calculate_speed():
    pass
    # dist = great_circle((latitude_before, longitude_before), (latitude, longitude)).m
    # return speed


if __name__ == "__main__":
    calculate_speed()
